[package]
name = "thetos_entry"
version.workspace = true
edition.workspace = true
description = "Macro for abstracting ThetOS firmware entry (e.g. #[entry])."

[lib]
proc-macro = true # enables the use of procedural macros